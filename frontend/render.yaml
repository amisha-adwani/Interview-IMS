services:
  # Frontend Static Site
  - type: web
    name: mini-ims-frontend
    runtime: static
    plan: free
    buildCommand: npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://interview-ims.onrender.com/api
